'use strict';

// import Reflux from "reflux"
// import requests from "superagent"
// import Immutable from "immutable"
// import ImmutableDate from "bloody-immutable-date"



// // Each action is leike an event channel for one specific event. Actions are called by components.
// // The store is listening to all actions, and the components in turn are listening to the store.
// // Thus the flow is: User interaction -> component calls action -> store reacts and triggers -> components update

// global.SettingsActions = Reflux.createActions([
//     "save",     // called by button settings dialogs
// ]);
